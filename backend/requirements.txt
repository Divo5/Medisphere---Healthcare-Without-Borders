# ─────────────────────────────────────────────────
#  Medisphere Backend – Python Dependencies
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
motor==3.4.0               # Async MongoDB driver
pymongo==4.7.2             # MongoDB driver (used by motor)

# Auth & Security
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # Password hashing
python-multipart==0.0.9            # File uploads

# Settings
pydantic==2.7.1
pydantic-settings==2.2.1

# AWS S3
boto3==1.34.84

# Email
# Uses built-in smtplib — no extra package needed

# AI / ML
numpy==1.26.4
Pillow==10.3.0
scikit-learn==1.4.2

# TensorFlow (for Eye CNN — uncomment when model weights are ready)
# tensorflow==2.16.1

# OCR (Tesseract — uncomment when Tesseract is installed on system)
# pytesseract==0.3.10
# pdf2image==1.17.0

# HTTP Client (for external API calls)
httpx==0.27.0
aiofiles==23.2.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0              # async test client

# Dev utilities
python-dotenv==1.0.1       # Load .env in scripts

# Payments
razorpay==2.0.1

